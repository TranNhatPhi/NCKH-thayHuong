# Cài: pip install -r requirements.txt
torch>=2.0
torchvision
numpy
rasterio
pyyaml
scikit-image                     # Otsu baseline
segmentation-models-pytorch      # U-Net++, DeepLabV3, SegFormer (backbone ResNet34/MiT)
spikingjelly                     # Spiking U-Net (SNN) + ANN2SNN
requests                         # spikingjelly.ann2snn cần (import requests)
ptflops                          # đếm MACs/FLOPs nhất quán (CNN + Transformer) cho đo năng lượng
scipy                            # Wilcoxon (analysis.py)
# torchao — cài riêng (không bắt buộc) trong setup_vast.sh cho INT4; xem quantize_int4.py
