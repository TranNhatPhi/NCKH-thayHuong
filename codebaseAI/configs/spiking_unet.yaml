_base_: base.yaml
name: spiking_unet_T4      # runs/spiking_unet_T4/
model: spiking_unet
batch_size: 8             # SNN nặng RAM hơn (nhân theo T bước); H100 94GB kham tốt
model_kwargs: {base: 32, T: 4, encoding: direct}   # ablation T ∈ {2,4,6,8}
